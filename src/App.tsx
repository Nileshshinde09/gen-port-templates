import React from "react";
import { Home } from "./pages";
const App = (): React.ReactNode => {
  return (
      <main className="w-full h-full">
        <Home/>
      </main>
  );
};

export default App;